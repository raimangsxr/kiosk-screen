# Index: C5-remote-control

Specs that touch the remote-control surface.

## Active

- `specs/020-display-control-rotation-tests/` — closes the
  deferred test work from 018 (pause/resume/fixed + new
  navigation commands + autodetect).
- `specs/019-display-control-canonical/` — canonical anchor for
  `display_control_state` and the four amending specs. Start here.

## Archived

- `specs/_archive/C1-kiosk-display-runtime/006-remote-control-display/`
  — foundational remote control.
- `specs/_archive/C5-remote-control/015-remote-control-polish/` —
  Material 3 page rewrite; shipped via `main` PR #10 (orphaned by
  reality; see `reality.md`).

## Capabilities touched

- Primary: C5 (remote control).
- Cross-capability: C1 (display runtime), C2 (top content for fixed
  mode), C7 (branding overlay integration).