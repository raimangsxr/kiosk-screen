# Supersedes: C5-remote-control

Cross-spec amendments within the C5 capability.

| From | To | Amendment |
|---|---|---|
| 006 | 015 | Material 3 page rewrite (orphaned by reality; design source only). |
| 006 | 016 | `selectedContentId` column replaced by `selectedIframeId`; `contentMode` enum unchanged. |
| 006 | 017 | Branding overlay integration on the kiosk (C7). |
| 006 | 018 | `contentMode` extended to `{loop, iframe, fixed}`; `selectedFixedContentId` added; `navigationCommand` extended with `pause`/`resume`; `display_control_paused`/`resumed`/`fixed_changed` audit events added. |
| 006 | 019 | When 019 lands, it becomes the canonical anchor for the four amendments. |