# Supersedes: C6-public-api-and-keys

Cross-spec amendments within the C6 capability.

| From | To | Amendment |
|---|---|---|
| 009 | 012 | `POST /api/admin/api-keys/{id}/delete` (hard delete, revoked only). |
| 009 | 018 | Public endpoint silently ignores `isFixed` and `recurringEveryXIterations`. |