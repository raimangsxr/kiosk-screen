# Index: C6-public-api-and-keys

Specs that touch the public API and API keys.

## Active

- None.

## Archived

- `specs/_archive/C2-content-and-ads/009-public-content-api/` —
  public upload + novelty queue + API key model.
- `specs/_archive/C3-admin-shell/010-admin-polish-bundle/012-…/` —
  delete revoked API keys.

## Active cross-references

- `specs/018-content-rotation-modes/` — public API silently ignores
  `isFixed` and `recurringEveryXIterations`.

## Capabilities touched

- Primary: C6 (public API and keys).
- Cross-capability: C2 (public upload creates `top_content_items`),
  C3 (admin event log renders the audit events).