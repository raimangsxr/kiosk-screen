# Index: C7-event-branding

Specs that touch event branding.

## Active

- `specs/017-event-branding/` — event configuration module,
  branding overlay, sponsor title.

## Cross-active

- `specs/018-content-rotation-modes/` — supersedes 017 US2 (overlay
  hidden in iframe mode); will land in C7 on closure.

## Archived

- None yet.

## Capabilities touched

- Primary: C7 (event branding).
- Cross-capability: C1 (kiosk renders the overlay), C3 (admin
  event configuration form), C4 (event configuration schema and
  audit event).