# Status: 015-remote-control-polish (orphaned by reality)

**Capability**: C5-remote-control
**Closed on**: 2026-06-22 (commit `sdd-optimization-bundle`) — see `reality.md`
**Branch declared in spec**: `015-remote-control-polish`
**Branch actually shipped**: `main` (commits listed in `reality.md`)

The Material 3 remote-control rewrite shipped via the `main` branch
under PR #10 (`66318c6 015 remote control polish (#10)`), not on the
`015-remote-control-polish` branch the spec declared. The spec's
`tasks.md` (392 lines, 22 tasks) was never used as the working
checklist; the actual implementation tracked the four commits listed
in `reality.md`.

**Status file index**: read `reality.md` for the commit pointers and
spec-vs-reality summary. The spec itself is preserved verbatim for
historical record.

**Open tasks at closure**: 22/22 unchecked. The spec is a
historical description, not a current checklist.