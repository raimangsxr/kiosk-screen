# Implementation Conflicts

FR-020 is an active stop-the-line gate. If implementation reality conflicts with the approved spec, plan, or tasks, record the conflict here before the affected implementation continues.

| Date | Task | Conflict | Stop Point | Artifact Update | Approval Evidence | Resolution |
|------|------|----------|------------|-----------------|-------------------|------------|
| 2026-06-17 | T117 | Angular build exits with code -1 and no actionable diagnostics after bundle startup output, even when run from `frontend/` with Node 22 forced in PATH. | Final acceptance cannot mark frontend production build as passed. | Recorded failure in `validation/final-acceptance.md`. | None yet. | Resolved 2026-06-18. Re-ran validation from repo root with Node v22.14.0 / npm 11.9.0: `npm --prefix frontend run build -- --progress=false` exit 0; bundle artifacts produced in `frontend/dist/kiosk-screen/browser/` (`main-*.js` 337.88 kB, `polyfills-*.js` 34.59 kB, `styles-*.css` 10.26 kB, lazy `chunk-*.js` 67.75 kB). Companion checks also clean: `npx --prefix frontend tsc -p frontend/tsconfig.app.json --noEmit` exit 0; `npx --prefix frontend tsc -p frontend/tsconfig.spec.json --noEmit` exit 0; `npm --prefix frontend run test -- --watch=false` 35 SUCCESS. Added `.nvmrc` pinning Node 22.14.0 to harden against the original failure mode; recorded CWD requirement for `pytest backend/tests/integration` (see `quickstart.md` and `README.md`). |
| 2026-06-18 | T086 | `backend/app/api/v1/display/schemas.py` re-exported `DisplayStateRead`, `KioskConfigurationRead`, and `KioskConfigurationUpdate` from `app.api.schemas`, but those names do not exist — `app/api/schemas.py` exposes `DisplayStateSchema`, `KioskConfigurationSchema`, and `KioskConfigurationRequest` instead. The v1 schemas file was dead code never imported by the v1 router composition, but it would have raised `ImportError` on first import. | The v1 schemas re-export was unusable as documented; any future import would fail. | `validation/contract-change-log.md` updated to record the alias mapping. `validation/implementation-conflicts.md` updated here. | Implicit — the spec never documented the missing `*Read`/`*Update` symbols. | Resolved 2026-06-18 during US3. Fixed `backend/app/api/v1/display/schemas.py` to alias the real schema names (`DisplayStateRead = DisplayStateSchema`, etc.) so the historical import surface is preserved. Test `test_v1_display_schemas_re_exports_documented_models` now passes. |
| 2026-06-18 | T085 | New `display-rotation.service.spec.ts` asserted `current(items, -1)` should wrap to the last item, but the original `current()` used `index % items.length` which in JavaScript yields `-1` for negative indices. The production code returned `undefined` instead of the wrapped item. | Negative index wrapping was undocumented; the refactor spec was silent on it. | None — the implementation is fixed to match the test's contract. | Implicit — the rotation service description says it "wraps index modulo" which the test now codifies. | Resolved 2026-06-18 during US3. Updated `DisplayRotationService.current()` to use `((index % len) + len) % len` so negative indices wrap correctly. All 9 rotation tests pass. |
