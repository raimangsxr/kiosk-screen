# Tasks: CHG-026 Cross-repo platform standardization

- [x] Add/refresh full local compose stack.
- [x] Parameterize frontend Dockerfile with `dev` and `prod` targets.
- [x] Convert backend Dockerfile to a non-root wheel runtime.
- [x] Add Docker ignore files for backend and frontend contexts.
- [x] Parameterize Angular dev-server proxy target for compose.
- [x] Add Python, npm, and Docker Buildx cache to release workflow.
- [x] Validate manifest, compose, and workflow YAML parse.
- [ ] Validate `docker compose config` when Docker is available.
