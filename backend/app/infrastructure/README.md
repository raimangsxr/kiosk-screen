# Backend Infrastructure Boundary

Infrastructure code owns adapters for persistence, media storage, observability, and other delivery details. Business behavior belongs in `backend/app/application/` or `backend/app/domain/`.
